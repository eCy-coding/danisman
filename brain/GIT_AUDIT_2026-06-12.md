# GIT AUDIT — eCyPro Repo Uçtan Uca Push/Yarım-İş Denetimi

> **Tarih:** 2026-06-12 · **Denetçi:** Claude (eCyPro v3.6 disiplini, kanıt-veya-sessizlik)
> **Kapsam:** pushlanmamış commit'ler, yarım kalmış işler, pushlanması gereken yerler — tüm repo.
> **Yöntem:** Tüm bulgular gerçek repo üzerinde deterministik araç çıktısından (`git status`,
> `git log --branches --not --remotes`, `git branch -vv`, `git stash list`, `git worktree list`,
> `ls .git/`). Simülasyon yok. Sandbox'tan SSH yok (verified: `Host key verification failed`)
> → push işlemleri host'ta (MacBook) koşulur. Remote refler taze: FETCH_HEAD 2026-06-12 10:20 UTC.

---

## 1. YÖNETİCİ ÖZETİ (verified)

| # | Bulgu | Boyut | Aciliyet |
|---|---|---|---|
| F1 | Aktif branch `fix/project-gaps-2026-06-08` **6 commit ahead, pushlanmamış** | 6 commit (gate-0/1/2 Perspektifler + 3 services mega-menü) | **P0** |
| F2 | **Stale `.git/index.lock`** (bugün 11:07 UTC, 0 byte) — sandbox silemiyor (`Operation not permitted`) | commit/stage işlemlerini bloklar | **P0** |
| F3 | Working tree **17 kirli dosya**: 3 husky hook'a worktree path sızıntısı + 11 üretilmiş XML (rss/sitemap) + blog-posts.json + 2 untracked (`istek.md`, `MAKRO_PROMPT.md`) | 732+/732- satır | **P1** |
| F4 | **88 unpushed commit** (tüm lokal branch'ler, hiçbir remote'ta yok) | 88 commit / ~30 branch | P1 |
| F5 | **18 stash** — en eskisi main pre-reset (May), yarım iş içerenler var | 18 kayıt | P2 |
| F6 | **~100 worktree, neredeyse tümü `prunable`** (.claude/worktrees/*, /tmp/*, Windsurf kopyası) | disk + karışıklık | P2 |
| F7 | **231 lokal branch** / 116 remote / **29 gone-upstream** (remote'u silinmiş) | hijyen borcu | P2 |
| F8 | `KVKK_BREACH_ALERT.md`: fix uygulanmış, **PR açık, no-auto-merge** → owner merge bekliyor | 1 PR | P1 (owner) |
| F9 | GATE-2 Playwright e2e (`menu.spec.ts`, 10 test × 3 tarayıcı) **kullanıcıda koşulmayı bekliyor** (PROGRESS.md) | doğrulama borcu | P1 (owner) |
| F10 | 69 adet `index.lock.*` çöp dosyası (May 16 + Jun 2 kalıntısı) | temizlik | P3 |

`main` senkron (0/0, son commit #228 bugün). TODO/FIXME yükü düşük: src+server'da 2 adet.

## 2. PUSHLANMASI GEREKENLER — TRİYAJ MATRİSİ (verified, `git branch -vv` + rev-list)

### 2.1 P0 — Aktif iş, temiz ileri (behind 0): hemen pushlanmalı
| Branch | Ahead/Behind | İçerik |
|---|---|---|
| `fix/project-gaps-2026-06-08` (HEAD) | **+6 / 0** | `2c25c07` gate-2 menü, `3317b38` gate-1 taxonomy, `c0ac13a` gate-0 recon, `8af969d`+`8e2ebdc`+`f482c23` services mega-menü |

### 2.2 P1 — Temiz ileri, küçük sapma: push adayı (içerik gözden geçir → push)
| Branch | Ahead/Behind | Not |
|---|---|---|
| `feat/per-url-seo-hardening` | +2 / 0 | temiz ileri |
| `fix/e2e-kvkk-consent-selector` | +2 / 0 | upstream: `sprint-11/e2e-fortification` |
| `sprint-13/r10-image-assets-e2e-verify` | +2 / 0 | temiz ileri |
| `feat/admin-phase-0-p0-triage` | **+18 / 9** | en büyük sapma — önce rebase kararı |

### 2.3 P2 — İleri ama çok geride (muhtemelen superseded): push ETME, önce karar
`feat/sentry-release-sourcemap` (+6/-101), `feat/real-data-seed…` (+4/-182), `feat/owner-free-batch-b3-b4-b5` (+3/-98),
`feat/perspektif-admin-category-mgmt` (+3/-114), `fix/live-bug-sweep-v2` (+2/-131), `feat/p36-t02-analytics-sse` (+2/-110),
`feat/sprint-h2-vercel-qa` (+2/-34), `feat/integration-outbox-wal` (+1/-15) ve +1 ahead olan ~15 eski branch
(docs/phase-33, p32/p34 serisi, contact/discovery-wire, remove-roi serisi, ssr-prerender-vercel, register-dead-crons,
kvkk-consent-intake-routes, helmet-shim, permanence-watchdog, pricing-3tier, services-content serisi, seo/p32-t02-t07).
**Karar kuralı:** içerik main'e cherry-pick/yeni PR ile mi taşınır, yoksa branch arşivlenir mi — branch başına 1 satır karar.

### 2.4 Lokal-only branch'ler (UPSTREAM YOK)
~60 adet `claude/*`, `pr-*-local`, sprint çalışma kopyası — 88 unpushed commit'in çoğu burada.
Çoğu squash-merge edilmiş işin kalıntısı. **Push değil, temizlik adayı** (merge kontrolü sonrası sil).

## 3. YARIM KALMIŞ İŞLER ENVANTERİ (verified)

1. **Working tree (F3):**
   - `.husky/{commit-msg,pre-commit,pre-push}` → lefthook fallback yolu `.claude/worktrees/condescending-brown-adcb54/...`'a sızmış. Ana repo hook'u prunable worktree'ye bağımlı = kırılgan. **Aksiyon: `git restore .husky/`** (worktree path'i ana yola döner).
   - `public/*.xml` + `src/data/blog-posts.json` → `gen:blog`/sitemap yeniden üretim churn'ü (+732/-732 simetrik). **Karar:** gate-3 içerik işiyle birlikte commit YA DA `git restore` (gürültü). Öneri: restore — gate-3'te zaten yeniden üretilecek.
   - `istek.md` + `MAKRO_PROMPT.md` (untracked) → Perspektifler master prompt teslimatları. **Aksiyon: commit** (brain/ veya kök; konvansiyon gereği kök).
2. **Stash triyajı (F5, 18 kayıt):** İncelemeye değer: `stash@{0}` r14-render-build-heap WIP, `stash@{3}` p44-t04 observability CI fix, `stash@{6}` per-url-seo 6 route, `stash@{7}` prod E2E smoke, `stash@{11}` phase0 verify (a11y+red-team), `stash@{12}` render build-blocker. Geri kalanı autostash/pre-reset yedeği → drop adayı. **Kural:** drop öncesi `git stash branch rescue/<n> stash@{n}` ile kurtarma mümkün.
3. **Owner kuyruğu:** KVKK PR merge (F8) · e2e menu.spec host koşumu (F9) · `index.lock` silme (F2).
4. **PROGRESS.md durumu:** GATE-0/1/2 DONE → **sıradaki iş Phase 3 (Hub `/perspektifler`)**. Push, Phase 3 başlamadan yapılmalı (6 commit tek PR'a şişmeden).

## 4. REMEDİASYON — HOST'TA KOŞULACAK KOMUTLAR (sıralı)

```bash
# A) Kilidi temizle (F2 + F10) — repo kökünde:
rm -f ~/Desktop/ecypro/.git/index.lock && rm -f ~/Desktop/ecypro/.git/index.lock.*
# (alternatif: mevcut UNLOCK_AND_COMMIT_P9.command zaten bu işi yapıyor)

# B) Hook sızıntısını ve üretilmiş dosya churn'ünü geri al (F3):
cd ~/Desktop/ecypro && git restore .husky/ public/ src/data/blog-posts.json

# C) Master prompt teslimatlarını commit'le (F3):
git add istek.md MAKRO_PROMPT.md MAKRO_PROMPT_GIT_AUDIT.md brain/GIT_AUDIT_2026-06-12.md
git commit -m "docs(brain): git audit 2026-06-12 + perspektifler/git-audit master promptlar"

# D) Push-readiness kapısı + push (F1) — lefthook pre-push typecheck+build koşar:
bash READY_TO_PUSH.command   # tüm kapılar yeşilse push komutunu basar
git push origin fix/project-gaps-2026-06-08

# E) Worktree + branch hijyeni (F6, F7) — güvenli sıra:
git worktree prune --verbose
git branch -vv | grep ': gone]' | awk '{print $1}' > /tmp/gone.txt && cat /tmp/gone.txt
# gözden geçir → merge edilmişleri sil: xargs -n1 git branch -d < /tmp/gone.txt  (-D YOK)
```

**Yapılmayanlar ve nedeni:** push/commit sandbox'tan yapılmadı (SSH yok + index.lock + Tier sınırı:
istek "tespit"). Dirty tree'ye dokunulmadı (tespit ≠ müdahale; komutlar yukarıda).

## 5. SONRAKİ ADIM
`MAKRO_PROMPT_GIT_AUDIT.md` (repo kökü) — bu denetimi her seans tekrar koşturup remediasyonu
uçtan uca yürütecek kopyala-yapıştır İngilizce master prompt. Phase 3 Hub işine dönmeden önce
§4 A-D adımları koşulmalı.
