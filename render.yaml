# ─── Render Blueprint (Infrastructure as Code) ────────────────────────────
# Deploy: render blueprint apply
# Docs:   https://docs.render.com/infrastructure-as-code
#
# BE-10 — Hybrid hosting: this manifest provisions the API service.
# Frontend is on Hostinger (static, www.ecypro.com).
# This service answers api.ecypro.com (CNAME → Render external URL).

databases:
  - name: ecypro-db
    plan: starter
    databaseName: ecypro
    user: ecypro_user
    region: frankfurt
    postgresMajorVersion: "17"
    ipAllowList: [] # Empty = Render internal network only

services:
  - type: web
    name: ecypro-api
    plan: starter
    runtime: node
    region: frankfurt
    rootDir: .
    # BE-9: prisma migrate deploy is now part of the build step so each
    # deploy applies pending migrations before the new server starts.
    buildCommand: |
      npm ci && \
      npx prisma generate && \
      npx prisma migrate deploy && \
      npx tsc -p tsconfig.server.json
    startCommand: node dist/server/index.js
    healthCheckPath: /api/health
    # P20 BE Aşama 4 — Post-deploy cache warm-up.
    # NOTE (P39): postDeployCommand requires plan: standard or higher.
    # On Starter we run the warm-up manually post-deploy via curl.
    # postDeployCommand: |
    #   sleep 5 && \
    #   curl -sf -X POST -H "Authorization: Bearer $CACHE_WARMUP_TOKEN" \
    #     "${CACHE_WARMUP_BASE_URL:-https://api.ecypro.com}/api/admin/cache/warmup" \
    #     || true
    autoDeploy: true
    branch: main
    numInstances: 1
    # NOTE (P39): autoscaling (scaling block) requires plan: standard+
    # Restore on plan upgrade.
    # scaling:
    #   minInstances: 1
    #   maxInstances: 3
    #   targetMemoryPercent: 75
    #   targetCPUPercent: 75
    envVars:
      # ── Required ───────────────────────────────────────────
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      - key: DATABASE_URL
        fromDatabase:
          name: ecypro-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CORS_ORIGIN
        value: https://www.ecypro.com,https://ecypro.com
      - key: TRUST_PROXY
        value: "1"

      # ── Observability ──────────────────────────────────────
      - key: SENTRY_DSN
        sync: false # Set in Render dashboard (per-environment value)
      - key: SENTRY_ENVIRONMENT
        value: production
      - key: SENTRY_TRACES_SAMPLE_RATE
        value: "0.1"
      - key: LOG_LEVEL
        value: info
      - key: LOGTAIL_SOURCE_TOKEN
        sync: false # Optional — set in dashboard if Better Stack used

      # ── Integrations (set in Render dashboard, never commit values) ──
      - key: REDIS_URL
        sync: false
      - key: CALCOM_API_KEY
        sync: false
      - key: CALCOM_EVENT_TYPE_ID
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        value: noreply@ecypro.com
      - key: GEMINI_API_KEY
        sync: false
      - key: WEBHOOK_HMAC_SECRET
        generateValue: true
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: HIBP_API_KEY
        sync: false

      # ── Rate-limit overrides (optional) ────────────────────
      - key: RATE_LIMIT_WINDOW_MS
        value: "900000" # 15 min
      - key: RATE_LIMIT_MAX
        value: "100"

      # ── P20: Connection pool sizing (Starter-tier defaults) ──
      # Formula: pool = (cpu_cores × 2) + 1   →   Starter 0.5 vCPU = ~5
      # We round up to 10 for Node async IO-bound headroom.
      # See outputs/P20_BE_POOL_MATH.md for the full derivation.
      - key: PG_POOL_MAX
        value: "10"
      - key: PG_POOL_MIN
        value: "0"
      - key: PG_POOL_IDLE_MS
        value: "30000"
      - key: PG_POOL_CONN_MS
        value: "30000"
      - key: PG_POOL_QUERY_MS
        value: "10000"
      - key: PG_STATEMENT_MS
        value: "15000"

      # ── P20: Node heap ceiling (V8 default 1.5 GB doesn't fit Starter) ──
      # Starter (512 MB total, ~380 MB app baseline) → cap V8 at 384 MB.
      # Upgrade to "1536" on Standard, "3072" on Pro.
      - key: NODE_OPTIONS
        value: "--max-old-space-size=384"

      # ── P20: Read replica (Standard+ only; leave unset on Starter) ──
      - key: DATABASE_URL_REPLICA
        sync: false

      # ── P20: Cache warm-up (post-deploy hook) ──
      - key: CACHE_WARMUP_TOKEN
        generateValue: true
