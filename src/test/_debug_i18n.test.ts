/**
 * P26 — Placeholder. This file used to host transient debug experiments
 * during the /services hang investigation. Kept as a no-op so the filename
 * doesn't get reused accidentally; vitest still needs at least one test
 * suite per file.
 */
import { describe, it, expect } from 'vitest';

describe('_debug_i18n placeholder', () => {
  it('exists so the file has a suite', () => {
    expect(true).toBe(true);
  });
});
